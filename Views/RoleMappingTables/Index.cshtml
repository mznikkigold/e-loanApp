@model IEnumerable<e_loanApp.Models.RoleMappingTable>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RoleTable.Role)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserTable.User_First_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RoleTable.Role)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UserTable.User_First_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RoleMappingId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RoleMappingId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RoleMappingId })
        </td>
    </tr>
}

</table>
